using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tesstt : MonoBehaviour
{
    public Test[] tests;
    public Test test;
    // Start is called before the first frame update
    void Start()
    {
        test = tests[0];
    }

    // Update is called once per frame
    void Update()
    {

    }
}
